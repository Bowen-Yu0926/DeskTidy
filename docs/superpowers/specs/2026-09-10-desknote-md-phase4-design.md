# deskNote Markdown 增强 · 四期设计

**日期：** 2026-09-10  
**状态：** 实现中（用户「继续」）  
**承接：** 三期笔记库侧栏 / 搜索  

## 范围

1. **图片粘贴/拖入**：保存到笔记旁 `assets/`，插入相对路径 `![](assets/…)`。  
2. **导出 Word**：调用本机 `pandoc`；未安装则提示。  
3. **字数统计**：状态栏显示（选区优先）。  

## 非目标

云图床、捆绑 Pandoc、LaTeX/Epub 全家桶。
